<beans:beans xmlns="http://www.springframework.org/schema/security"
xmlns:beans="http://www.springframework.org/schema/beans"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd">
		
<!-- Cấu hình login, logout, phân quyền người dùng -->

	<http use-expressions="true">
		<csrf disabled="true"/>

        
        
        <!-- <intercept-url pattern="/admin/cat/**" access="hasAnyRole('ROLE_MOD', 'ROLE_ADMIN')" />
        <intercept-url pattern="/admin/news/**" access="hasAnyRole('ROLE_MOD', 'ROLE_ADMIN')" />
        
        <intercept-url pattern="/admin" access="hasAnyRole('ROLE_MOD', 'ROLE_ADMIN', 'ROLE_USER')" />
        
        <intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" /> -->
        <intercept-url pattern="/admin/**" access="hasRole('ROLE_admin')" />
        <intercept-url pattern="/hello" access="hasAnyRole('ROLE_admin','ROLE_user')" />
        <intercept-url pattern="/**" access="permitAll()" />
 
        <access-denied-handler error-page="/403" />
 
        <form-login login-page='/login' login-processing-url="/login"
            default-target-url="/home" always-use-default-target="true"
            authentication-failure-url="/login?error=loginErr" 
            username-parameter="username"
            password-parameter="password" />
 
        <logout logout-url="/logout" logout-success-url="/login"
            delete-cookies="JSESSIONID" invalidate-session="true" />
 
    </http>
    
    <authentication-manager>
    <authentication-provider>
            <user-service>
                <user name="Tom1" password="123456" authorities="ROLE_admin" />
                <user name="admin1" password="12345" authorities="ROLE_admin ROLE_user" />
            </user-service>
        </authentication-provider>
    
        <!-- authentication from database -->
        <authentication-provider>
           	<password-encoder hash="md5" />
            
            <jdbc-user-service data-source-ref="myDataSource"
                users-by-username-query="select username,password from Account where username=?"
                authorities-by-username-query="select username, concat('ROLE_',role) role from Account where username=?" />
        </authentication-provider>
    </authentication-manager>

</beans:beans>